# 메트릭 실측 결과 (2026-07-20)

검색이 아니라 **제출킷 코드를 그대로 import 해서 직접 측정**한 결과. 재현 스크립트는 `tools/`.
metric 진단 probe: train 데이터에서 평가 형식(첫 프레임 + (16,6) 액션 + 정답 16프레임)으로 48개 샘플 추출.
이 probe는 현재 학습 목록과 겹치므로 **checkpoint 선택용 holdout이 아니다**(METHOD.md 10-2).

> 이 결과들은 작은 표본과 특정 extractor에 대한 **반증 가능한 관찰**이지 고정된 계획이나 보편 법칙이 아니다.
> 특히 eval 입력을 보고 얻은 관찰은 규정상 학습·하이퍼파라미터·모델 선택에 사용하지 않고 격리한다.

```bash
uv venv --python 3.10 .venv
uv pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu126
uv pip install pytorch-lightning==1.9.3 einops==0.6.0 "av>=10" imageio timm omegaconf pandas pyarrow
.venv/bin/python tools/build_valset.py --num-samples 48
cd tools && ../.venv/bin/python experiments.py --limit 32
```
※ `omegaconf`는 제출킷 requirements.txt에 빠져 있으나 action_extractor.ckpt 로드에 필요하다.

**표본 수 (절마다 다르므로 주의)**: 검증셋 자체는 48개지만 실제 사용 수는 아래와 같다.
1·2·3절 32클립 / 4-1절 32클립 / 4-3절 데이터셋 128개 × 4클립 / 6절 24클립 /
움직임 민감도(1절 3번째 행) 12에피소드 × 4구간 / 8절 정렬 검사 1,195에피소드(train parquet 전수 계열).
1~3절 수치는 32클립 평균이라 소수 셋째 자리는 표본오차 범위로 봐야 한다.

## 1. 기준선 지도 (cosine distance, 낮을수록 좋음)

| 예측 | DINO | Video | Action MAE |
|---|---|---|---|
| 정답 그대로 | 0.0000 | 0.0000 | 1.2062 |
| **첫 프레임 정지 반복** | **0.1095** | **0.0780** | 1.2470 |
| 같은 장면, 다른 구간 (움직임 오답) | 0.2640 | 0.1278 | 1.2668 |
| 다른 장면 (외형까지 오답) | 0.6828 | 0.3178 | 1.2646 |

**해석**
- 이 probe에서는 외형이 틀린 경우(0.68)가 움직임이 틀린 경우(0.26)보다 2.6배 큰 손해였다.
  시작 프레임 충실도를 강한 기본 가설로 두되 다른 holdout의 공식 가중 점수로 다시 확인한다.
- **정지 영상(0.1095)이 움직임 오답(0.2640)보다 좋다.** 이 probe에서는 어설픈 움직임이 정지보다 손해였다.
  다만 여기서 "움직임 오답"은 같은 에피소드의 **멀리 떨어진 구간**이라 팔 위치가 크게 다른, 오답 중에서도
  강한 오답이다. 실제 모델의 오차는 이보다 작을 수 있으므로 "보수적 motion이 항상 낫다"로 일반화하면 안 된다.
- 위 두 줄은 train probe에서 나온 관찰이며, eval 장면 분포를 근거로 삼지 않는다(4-4의 격리 원칙).
  모델의 존재 가치 기준선은 **같은 probe에서 정지 영상 0.1095를 넘는지**로 판정한다.

## 2. 검사한 후처리는 이 probe에서 전부 손해

| 후처리 | DINO | Video | Action MAE |
|---|---|---|---|
| 없음(정답) | 0.0000 | 0.0000 | 1.2062 |
| 공간 블러 σ=1 | 0.0222 | 0.0168 | 1.2087 |
| 공간 블러 σ=3 | 0.2345 | 0.1201 | 1.2288 |
| 공간 블러 σ=6 | 0.4811 | 0.3436 | 1.2456 |
| 시간축 이동평균 3 | 0.0878 | 0.0289 | 1.2050 |
| 시간축 이동평균 5 | 0.1384 | 0.0494 | 1.2125 |

→ 이 설정에서는 **블러·스무딩이 모든 컴포넌트에서 손해**였다. DINOv2 CLS는 블러에 매우 민감했다
(σ=3이면 이미 정지 영상보다 나쁨). 다른 생성 모델에서도 자동으로 성립하는 법칙은 아니다.

## 3. 픽셀 앙상블 평균은 역효과 (통념과 반대)

디퓨전 샘플 편차(시간 지터 ±1프레임, 공간 이동 ±3px, 노이즈)를 모사해 N개 평균:

| 예측 | DINO | Video |
|---|---|---|
| 단일 샘플 | **0.0830** | 0.1058 |
| 2개 평균 | 0.1456 | 0.0864 |
| 4개 평균 | 0.2030 | 0.0814 |

→ 1X World Model Challenge 우승 트릭인 "예측 앙상블 평균"은 **PSNR 기준**이라 이 대회에 그대로 오면 안 된다.
정렬이 어긋난 샘플을 픽셀 평균하면 잔상이 생기고 DINO가 크게 싫어한다(0.083 → 0.203).
Video feature는 미세하게 좋아지나 DINO 손해가 압도적이다.

**단서 — 이 결과는 시뮬레이션이다.** 실제 디퓨전 샘플이 아니라 내가 가정한 섭동 모델
(시간 지터 ±1프레임, 공간 이동 ±3px, 가우시안 노이즈)로 만든 가짜 샘플을 평균한 것이다.
결론의 강도는 이 섭동 모델이 실제 샘플 분산을 얼마나 닮았는지에 달려 있다. 특히 **공간 이동 ±3px**이
잔상의 주원인일 가능성이 높은데, 잘 학습된 모델의 샘플 간 편차는 이보다 정렬이 좋을 수 있다.
따라서 현재 지위는 "픽셀 평균 금지"가 아니라 **"기본값으로 쓰지 말고, 실제 샘플로 재검증한 뒤에만 쓴다"**이다.
latent 공간 평균과 best-of-N 선택은 별개 문제로 미검증.

## 4. Action Component — train probe와 eval 제출물의 동작 차이

### 4-1. 다양한 train 장면에서는 extractor가 domain-sensitive하다
정답 영상인데도 MAE 1.2062. 정규화 공간에서 **전역 평균(0)을 찍는 자명한 예측이 0.8047**이므로 그보다 나쁘다.

| 측정 | MAE |
|---|---|
| 추출기 그대로 | 1.2062 |
| 전역 평균 예측 | 0.8047 |
| 전역 편향 제거 후 | 0.9820 |
| **샘플별(장면별) 편향 제거 후** | **0.3533** |

차원별 상관: dim0 −0.06, dim1 +0.33, dim2 +0.33, dim3 −0.08, dim4 −0.42, dim5 +0.14
데이터셋별 편차도 크다 (vladfatu/so100_office 0.72 ~ mikechambers/block_cup_14 2.04).

→ 클립 내 상대 움직임은 읽지만(오프셋 제거 시 0.35) **장면별 절대 오프셋을 못 맞춘다.**
카메라 외부 파라미터가 데이터셋마다 달라 영상만으로 절대 관절각이 결정되지 않기 때문으로 보인다.
내 검증셋은 48개 서로 다른 데이터셋에서 하나씩 뽑아 다양성을 극대화했고 여기엔 손목뷰·1080p 같은
비정형 세팅도 섞여 있다.

### 4-2. ★ eval 정지 제출물에서는 더 작은 오차가 관찰됐다

> **포렌식 격리:** 아래 eval 수치는 이미 만든 제출물의 현상을 이해하기 위한 기록일 뿐이다. 데이터 선택,
> action 표현, 학습 정책, 하이퍼파라미터 또는 checkpoint 선택의 근거로 사용하지 않는다.

정지 영상 216개를 만들어 **공식 제출킷을 그대로 돌려** 얻은 Action MAE:

```
mean 0.4305 | std 0.2163 | min 0.0457 | max 1.0913
```

같은 액션 파일에 대한 자명한 예측들의 MAE(정규화 공간):

| 자명한 예측 | eval MAE |
|---|---|
| 전역 평균(0) | 0.9273 |
| eval 액션 전체 평균 | 0.7788 |
| **첫 스텝 유지 (정지 영상의 이상적 값)** | **0.3369** |
| 클립 평균 유지 | 0.2083 |
| 실측: 정지 영상 제출 | 0.4305 |

→ 추출기가 첫 프레임 자세를 읽어 **이상값 0.3369에 0.09 오차로 근접**한다. 해당 eval 장면들에서는
정지 영상의 자세를 어느 정도 읽는다는 관찰이다. `0.43 → 0.1대` 같은 개선 폭은 GT 영상 없이 검증할 수
없는 낙관적 시나리오이므로 목표치로 사용하지 않는다.

**정정**: train probe 기반의 "Action Component는 둔감하다"는 결론을 eval에 옮길 수 없다.
액션 충실도를 측정해야 한다는 근거는 이 eval 분석이 아니라 공개된 공식 가중치 0.4와 train-only holdout이다.
검증셋과 eval의 차이는 카메라·로봇 외형 등 domain 차이일 수 있으나 원인은 아직 확인되지 않았다.
eval 외형에 맞춰 검증셋을 다시 고르는 것은 leakage 소지가 있으므로 하지 않는다. 대신 eval을 보지 않고
train metadata로만 정의한 카메라/해상도/업로더 strata와 데이터셋 단위 교차검증으로 강건성을 확인한다.

### 4-3. ★ 데이터셋 128개 전수 측정 — 캘리브레이션 관례가 제각각이다
`tools/rank_datasets.py`로 데이터셋마다 4클립씩 추출기 정확도를 측정 (`results/dataset_ranking.csv`):

| | action_mae (원본) | mae_debiased (클립별 오프셋 제거) |
|---|---|---|
| 최고 (aractingi/push_cube_offline_data) | 0.6475 | 0.1564 |
| 상위권 대표값 | 0.69 ~ 0.98 | 0.24 ~ 0.35 |
| 최악 (pierfabre/chicken) | 1.9912 | 0.4303 |

- 원본 MAE는 0.65~1.99로 3배 넘게 흩어지는데 **오프셋 제거 후에는 0.16~0.66으로 수렴**한다.
- MAE<0.8인 데이터셋은 128개 중 **5개뿐**(에피소드 618개, 프레임 41,200개)이다.

**→ 설계 가설: 절대 관절각 단독 조건은 calibration offset에 취약할 수 있다.**
이 측정만으로 영상 모델에서 absolute가 반드시 실패한다고 증명되지는 않는다. 시작 이미지가 offset을 추론하게
될 수도 있고 extractor의 오차와 실제 물리 calibration 오차도 동일하지 않다. 따라서 대안은 challenger로 둔다.

1. 첫 스텝 대비 anchor delta, 인접 step delta, absolute를 동일예산으로 비교한다.
2. absolute와 anchor delta를 함께 주는 `delta_anchor`로 시작 자세 정보를 명시한다.
3. dataset-ID embedding/가중 curriculum은 unseen dataset에서 정의가 불안정하므로 group holdout에서 이득을
   증명할 때만 사용한다.

### 4-4. eval/train 최근접 탐색 (포렌식 전용, 학습 결정 사용 금지)
DINOv2 feature로 train 1,536프레임(128 데이터셋 × 최대 12에피소드)에 대해 최근접 탐색:
- eval 최고 유사도 **0.781** (중앙값은 그보다 한참 아래)
- 반면 **같은 장면(동일 데이터셋 내 다른 에피소드)의 유사도는 평균 0.84~0.96**

→ 표본 범위에서는 동일 장면의 증거를 찾지 못했다. 다만 이것은 전수 동일성 증명이 아니며,
**이 결과로 train 데이터를 고르거나 학습 정책을 정하지 않는다.** `results/eval_source_match.json`과
`valset_evallike/`는 제출물 분석용으로만 격리한다.

## 5. ★ 제공된 베이스라인은 붕괴한다 — 정지 영상보다 나쁘다

> **포렌식 격리:** 아래는 이미 실행한 eval 제출물의 기록이다. 백본 크기·동결 정책·학습률·checkpoint를
> 고르는 근거로 되돌려 쓰지 않는다. 붕괴 여부와 정지 기준선 비교는 train-only group holdout에서 재현한다.

DynamiCrafter backbone(9.8GB)을 받아 `baseline_diffusion.ckpt`로 eval 216개를 실제 생성해 봤다.

**추론 시간: 14분 47초 / 216샘플 = 샘플당 4.1초** (H100 1장, DDIM 50 step, batch 1).
→ 1시간 제한 대비 25%만 쓴다. 다만 이는 **11M UNet에 대한 값이며 1.44B 모델로 외삽할 수 없다.**
UNet이 130배 커지고 attention 해상도도 그대로이므로 샘플당 예산 16.7초를 넘길 가능성이 실재한다.
DynamiCrafter 공식 보고도 512 모델 50-step을 A100에서 샘플당 약 20초로 적고 있어, 그대로면
216개에 약 72분으로 **한도 초과**다. full backbone의 216개 dry-run 실측이 P0이며 아직 하지 않았다.

| 제출물 | eval Action MAE | 프레임간 픽셀 변화량 |
|---|---|---|
| 첫 프레임 정지 | **0.4285** | 0.002 |
| 제공 베이스라인 | 0.5858 | 7.467 |

※ 같은 정지 영상인데 4-2절은 0.4305, 여기는 0.4285다. 4-2는 `make_submission_csv.py`가 쓴 CSV 값
(feature 정밀도 6자리 반올림)이고 여기는 별도 스크립트로 추출기를 직접 돌린 값이다.
두 경로의 배치 구성이 달라 생긴 차이로 보이며 **원인을 특정하지 않았다.** 제출 기준값은 CSV 쪽(0.4305)이다.

- **베이스라인이 정지 영상보다 Action MAE가 나쁘다.**
- 베이스라인과 정지 영상 사이의 DINO 거리는 **0.5809**다. 다만 이것은 두 제출물 사이의 거리일 뿐
  **정답까지의 거리가 아니다.** eval GT 영상이 없으므로 베이스라인의 실제 DINO/Video 점수는 모른다.
  "정지 영상이 정답에서 0.11"은 train probe 값이므로 eval에 그대로 옮길 수 없다.
- 육안 확인: t=0은 입력에 충실하지만 **t=5부터 로봇팔이 노이즈 구름으로 붕괴**하고 배경만 남는다.

→ **이 eval 실행에서 11M UNet 베이스라인은 붕괴했다.** Action(0.4285 vs 0.5858)에서는 정지 영상이 낮았고,
   육안상 외형도 t=5 이후 무너지므로 DINO/Video에서도 뒤질 개연성이 크지만 **확인된 것은 Action뿐이다.**
   합산식은 공식적으로 `0.3 DINO + 0.3 Video + 0.4 Action`이므로 역추정할 필요는 없다.
   이 관찰 자체로 다른 백본이나 학습 설정이 개선된다고 결론내리지 않는다.

## 6. 출력 기하를 틀리면 공짜로 손해

eval 입력 640×480 → 평가 캔버스 320×512에서 **실제 내용은 427×320, 좌우 검은 띠 42/43px**.

| 출력 기하 | DINO | Video |
|---|---|---|
| letterbox 정상 | 0.0000 | 0.0000 |
| 캔버스 꽉 채워 늘림 | 0.0352 | 0.0381 |
| 중앙 크롭 | 0.0653 | 0.0648 |

→ 다른 백본(Wan, LTX 등)을 쓸 때 **반드시 검은 띠를 포함한 4:3 내용을 320×512 캔버스에 렌더링**해야 한다.
베이스라인은 학습 데이터로더도 pad=True라 자동으로 맞지만, 백본 교체 시 놓치기 쉬운 함정이다.

## 7. 실행 지침 요약

1. 이 32클립 train probe에서는 시작 프레임 보존이 큰 영향을 보였다(외형 오답 0.68 vs 움직임 오답 0.26).
2. 같은 probe에서는 정지 영상 0.1095가 잘못된 움직임 0.2640보다 나았다. 보수적 motion은 후보 전략이지
   모든 액션에 적용할 절대 규칙이 아니다. 비교 대상이 "멀리 떨어진 구간"이라 강한 오답이었던 점도 감안한다.
3. 블러·시간축 스무딩은 현 probe의 기본 후보에서 제외한다. **픽셀 앙상블 평균은 섭동 시뮬레이션
   근거이므로 기본값으로 쓰지 않되, 실제 샘플로 재검증하기 전에는 완전 배제도 하지 않는다**(2·3절).
4. 출력은 320×512 캔버스 + 427×320 내용 + 좌우 검은 띠, 정확히 16프레임, 6fps.
   이 4번은 평가 코드에서 직접 확인한 것이라 이 문서에서 확신도가 가장 높다.
5. 공식 Action 가중치가 0.4이므로 train-only holdout에서 액션 충실도를 별도로 측정한다. 4-2의 eval 수치는
   학습 의사결정에 사용하지 않는다.
6. absolute·anchor delta·step delta와 시간 shift를 비교한다. calibration 차이 때문에 상대 표현이 유력하지만
   정확한 변환은 아직 미확정이다(4-3, 8).
7. 로컬 probe의 Action 지표는 eval을 대변하지 못한다(4-1~4-3). 모델 선택은 현재 probe나
   `results/dataset_ranking.csv` 상위셋이 아니라, 학습에서 완전히 빠진 group holdout 여러 개로 한다.
8. 정지 영상 베이스라인이 이미 제출 가능한 상태다 (`tools/make_static_baseline.py` → 제출킷 실행 완료).
   제출 여부는 대회 운영 전략으로 별도 결정하고, eval 입력 분석을 반복 튜닝 루프로 쓰지 않는다.

## 8. ★ 새로 찾은 P0: action과 관측 state의 한 프레임 시차 가능성

`action[t]`가 어느 관측에 대응하는지 train parquet만으로 다시 검사했다. 전체 11,132개 중
고정 seed로 1,200개 에피소드를 뽑았고, 길이 조건을 만족한 1,195개에서 모든 프레임·6개 관절의
raw MAE를 합산했다. `lag=+1`은 `action[t]`와 `state[t+1]`을 비교한다는 뜻이다.

```bash
.venv/bin/python tools/check_action_state_lag.py
```

| state lag | -3 | -2 | -1 | 0 | **+1** | +2 | +3 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| MAE (degree) | 8.7356 | 6.7479 | 4.6891 | 2.5951 | **1.7038** | 3.5436 | 5.5858 |

에피소드별 최적 lag도 `0: 5개`, **`+1: 1,168개`**, `+2: 20개`, `+3: 2개`였다.
즉 이 데이터의 action은 동시 관절 상태라기보다 다음 관측을 향한 target command라는 증거가 강하다.

변화량에서도 같은 방향이 나온다(에피소드별 MAE 평균).

| 비교 | 현재 시점끼리 | action을 1칸 앞선 원인으로 정렬 |
|---|---:|---:|
| 첫 시점 기준 누적 변위 | 3.3941 | **2.5010** |
| 인접 step 변화량 | 1.2496 | **0.7612** |

하지만 이것만으로 제출 모델의 최적 정렬이 확정되지는 않는다.

- 현재 데이터로더는 영상 frame `t`와 action `t`를 그대로 짝짓고, 추론은 첫 출력 프레임을 입력 이미지로
  고정한다. 따라서 16개 action 중 마지막 command가 영상 안에서 직접 관측되지 않는 **horizon mismatch**가 생길 수 있다.
- 반대로 공식 Action Component는 물리 state가 아니라 고정된 3D-CNN+BiGRU extractor의 출력을 채점한다.
  `tools/check_alignment.py`로 extractor가 비교적 잘 작동하는 train 60클립을 검사했을 때 debiased MAE는
  shift -2/0/+2가 각각 0.8560/0.8582/0.8633으로 거의 평평했다. 이 실험만으로 정렬을 판정할 수 없다.
- 현재 `delta`는 문헌에서 흔한 `a_t-a_{t-1}`가 아니라 **anchor displacement `a_t-a_0`**다.
  “delta가 좋다”는 정책 문헌은 상대 표현의 방향을 지지할 뿐, 이 정확한 변환을 검증하지 않는다.
- 현재 `delta_step` 구현은 anchor용 std를 재사용한다. 동일 sampler에서 step std는 anchor std의
  차원별 약 16~29%였으므로, mode별 정규화 통계 없이 얻은 비교 점수는 채택 근거로 쓰지 않는다.

따라서 84시간 본 학습 전에 동일한 train-only 고정 manifest와 짧은 예산으로 다음을 비교한다.

1. absolute, 현재 anchor delta, step delta를 비교한다.
2. 각 표현마다 same-index와 causal shift(`[0, a_0, ..., a_14]` 계열)를 비교한다.
3. anchor delta를 shift할 때 첫 command 효과가 사라질 수 있으므로 `delta_anchor`도 함께 본다.
4. diffusion val loss만 보지 말고 같은 생성 seed의 DINO/Video/Action, 그리고 action permutation 시
   생성 영상이 실제로 달라지는지를 함께 본다.

현재 증거로는 **“shifted delta가 정답”이 아니라 “정렬 ablation 없이 장기 학습을 시작하면 안 된다”**가 결론이다.

## 9. 공식 가중치를 반영한 점수 해석

공식식은 `0.3 DINO + 0.3 Video + 0.4 Action`이다. 서로 다른 train/eval 표본의 component를 섞어 만든
가상 합계는 리더보드 추정도 아니고 모델 선택 근거도 아니므로 계산하지 않는다.

시각 손해와 액션 이득의 의사결정은 반드시 가중합으로 한다. 예를 들어 DINO와 Video가 각각
`ΔD`, `ΔV`만큼 악화된 후보는 Action MAE가 최소 `0.75×(ΔD+ΔV)`보다 더 개선되어야 총점상 이득이다.
따라서 “외형이 무조건 최우선” 또는 “Action 가중치가 가장 크니 움직임이 최우선” 어느 쪽도 절대 규칙이 아니다.
